import styles from "./getstarted.module.scss";

export default function GetStarted() {
  return (
    <div id="pricing" className={styles.component}>
      <h2>Get started today!</h2>
      <button>PICK A PLAN</button>
    </div>
  );
}
